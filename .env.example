# Solutions Rent Car — variables de entorno
# Copia este archivo a .env (en Hostinger: hPanel → PHP → Variables de entorno)
# y rellena con los valores reales. NO subir .env al repositorio.

# Base de datos MySQL
DB_HOST=srv500.hstgr.io
DB_PORT=3306
DB_USER=u144787244_solutionsrent
DB_PASS=cambia_esto
DB_NAME=u144787244_solutionsrent
DB_SOCKET=

# JWT — secret aleatorio largo (64+ caracteres). Generar con:
#   php -r "echo bin2hex(random_bytes(48));"
# Si se deja vacío, se usará el archivo persistente runtime/jwt_secret.
JWT_SECRET=
